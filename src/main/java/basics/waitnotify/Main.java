package basics.waitnotify;

/**
 * wait() - освобождает ключ, удерживаемый потоком и переводит поток в состояние ожидания. Единственный способ вывести
 * его из прострации - метод notify().
 * notify() - приводит поток в чувство. Если ожидающих потоков много - рандомно выбирает один из них.
 * Эти методы не могут вызываться все блока synchronized.
 */
public class Main {
    public static void main(String[] args) {

    }
}
